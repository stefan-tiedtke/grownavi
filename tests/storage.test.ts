import{describe,expect,it,vi}from"vitest";import{demoProject,exportProjects,importProjects,LEGACY_STORAGE_KEY,loadProjects,saveProjects,STORAGE_KEY}from"@/lib/storage";
describe("lokale Grow-Daten",()=>{it("speichert und lädt Projekte",()=>{let value:string|null=null;const storage={getItem:vi.fn(()=>value),setItem:vi.fn((_k:string,v:string)=>{value=v})};saveProjects([demoProject],storage);expect(storage.setItem).toHaveBeenCalledWith(STORAGE_KEY,expect.any(String));expect(loadProjects(storage)[0].name).toContain("Waldlicht")});it("übernimmt bestehende Daten vom früheren Speicherschlüssel",()=>{const raw=JSON.stringify([demoProject]);expect(loadProjects({getItem:(key)=>key===LEGACY_STORAGE_KEY?raw:null})[0].name).toContain("Waldlicht")});it("exportiert und importiert verlustfrei",()=>{const raw=exportProjects([demoProject]);expect(importProjects(raw)[0].entries).toHaveLength(5)});it("weist ungültige Importe zurück",()=>expect(()=>importProjects('{"foo":1}')).toThrow("kein gültiger"));it("liefert bei defekten Daten das Demo zurück",()=>expect(loadProjects({getItem:()=>"kaputt"})[0].demo).toBe(true))});
